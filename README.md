## Getting Started

Create a mysql database and insert the schema into the database as LeagueStatsInterval.
Create a config.py file with a RIOT_API_KEY = "KEY" save and run the collect_summoners.py file (can take some time) then run the DataCollector.py.
